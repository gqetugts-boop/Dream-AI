# 幻梦圆环 · macOS 使用说明

在 Photoshop 画布上按住 **⌥ 再点右键**（或按 **⌥⌘R**），弹出一个饼菜单圆环，
不离开画布就能完成「生成 / 改参数 / 套预设 / 读选区」。

> **只想快点用上？** 双击 `安装.command`，再按它最后打印的提示做两步就行。
> 出问题就点菜单栏 **◎ → 自检…**，它会直接告诉你哪儿不对、该点哪里。

它由两部分组成，**两个都要装**：

| 部件 | 是什么 | 位置 |
|---|---|---|
| 幻梦AI 修图插件 | Photoshop 面板（UXP），圆环的所有实际操作都由它执行 | `Dream AI/Dream-ps-ai` |
| 幻梦圆环 | 菜单栏小助手，负责画圆环、接快捷键、当 WebSocket 服务端 | `Dream AI/HuanmengRing` |

两者通过 `ws://127.0.0.1:8799` 通信，只绑回环地址，**不经过网络、不对外暴露**。

---

## 一、安装（双击一个文件就行）

在访达里进到 `HuanmengRing` 文件夹，**双击 `安装.command`**。

它会自动做完这四件事，然后告诉你还差什么：

1. 构建助手
2. 关掉旧实例并启动
3. 设置开机自启
4. 跑一遍自检并打印结果

> macOS 可能问「是否允许打开」——点「打开」。这是本地生成的脚本，
> 不是从网上下载的，没有安全风险。

跑完之后，通常只剩两件事要你手动做（都是一次性的）：

### 还差 1：授权辅助功能（只有 ⌥右键需要）

**没有这个权限也能用 ⌥⌘R**，只是 ⌥右键那一路不通。

- 点菜单栏的 **◎** → 那一行「**⚠️ 点这里授予「辅助功能权限」**」
- 它会**直接跳到**系统设置的辅助功能页（不用自己翻三层菜单）
- 在列表里勾上 **HuanmengRing**

> ⚠️ **列表里显示的是文件名 `HuanmengRing`，不是 App 显示名「幻梦圆环」。**
> 这个 App 两个名字不一致，实测确认系统设置显示的是文件名。
> （菜单栏、Dock 里看到的是「幻梦圆环」，别拿那个去列表里找。）

**列表里没有它？** 按这个顺序做，一般第一条就好了：

1. **把「系统设置」整个退出（⌘Q）再重新打开。**
   那个列表是窗口打开那一刻加载的 —— 之后新登记的程序**不会自己冒出来**。
   这是最常见的原因。
2. 点菜单栏 **◎ →「⚠️ 点这里授予辅助功能权限」**。
   一个程序要在列表里出现，前提是它**至少请求过一次**。
   > 别用终端跑 `--request-permission` 那条命令：从终端启动时，
   > 系统可能把请求算在**终端**头上而不是助手头上，登记的是终端。
   > 菜单栏那条路径才是助手自己发起的。
3. 还不行就清掉旧记录重来一次：
   ```bash
   tccutil reset Accessibility com.huanmeng.ring
   ```
   然后重新点一次菜单栏那一行（重启助手也行）。

> **勾完不用重启助手** —— 它每 1.5 秒自己看一眼，授权生效后会弹一个提示，
> 菜单栏那行也会变成「已授权 ✓」。

#### 为什么以前每次都要重新授权

辅助功能授权是绑在**签名身份**上的，而签名身份有两种：

| 签名方式 | 身份是什么 | 重新构建后 |
|---|---|---|
| ad-hoc（默认，`codesign -s -`） | **二进制指纹**（cdhash） | 指纹变了 → 系统当成另一个程序 → **要重新授权** |
| 证书签名 | **证书指纹** | 指纹不变 → **不用再授权** |

之前还有一个更要命的问题：**开机自启指向的是 `native/build/` 里的构建产物**。
那是每次编译都会被整个替换掉的临时产物 —— 于是每编译一次，
自启启动的就是「另一个程序」，授权自然每次都失效。
（`bash native/autostart.sh status` 现在会直接把这个指出来。）

现在已经改成：**装到 `~/Applications/HuanmengRing.app`，自启指向那里**。
只要你不重新安装，授权一直有效。

#### 想连「重新构建 + 重新安装」都不用再授权

跑一次这个 —— **不需要输密码，不需要点任何授权框**：

```bash
bash "/Users/zero/Documents/Dream AI/HuanmengRing/native/make-signing-cert.sh"
```

它会生成一张**自签名代码签名证书**，之后 `build.sh` 自动用它签名，
签名身份就固定成「证书指纹」了 —— 以后再编译、再安装，授权都不会失效。

怎么知道真的固定了？比两次构建的签名身份：

```bash
cd "/Users/zero/Documents/Dream AI/HuanmengRing"
bash native/build.sh >/dev/null && codesign -d -r- native/build/HuanmengRing.app 2>&1 | tail -1
bash native/build.sh >/dev/null && codesign -d -r- native/build/HuanmengRing.app 2>&1 | tail -1
```

两行输出应当**一模一样**（`certificate root = H"…"`）。
ad-hoc 签名的话这里每次都是不同的 `cdhash H"…"` —— 那正是授权会失效的原因。

> 换证书之后**还要再授权最后一次**：签名身份刚从「二进制指纹」变成「证书」，
> 对系统来说这是个新身份。这一次之后就不用再管了。
>
> 撤销：`security delete-keychain ~/Library/Keychains/huanmeng-signing.keychain-db`，
> 再跑一次 build.sh 就退回 ad-hoc 签名。

### 还差 2：在 Photoshop 里加载插件

1. 打开 **UXP Developer Tool (UDT)**
2. **Add Plugin** → 选 `Dream AI/Dream-ps-ai` → **Load**
3. **顺手把 `com.huanmeng.ai.satellite` / `com.huanmeng.ai.satellite.probe` Unload 掉**
   —— 这是旧的「卫星插件」，它会和主插件**抢 8799 端口**，
   是「圆环显示未连接」最常见的原因
4. 在 PS 里打开：**插件 → 幻梦AI 修图插件**

### 确认连上了

菜单栏图标：**◎ = 插件已连上**，**○ = 还没连上**，**◐ = 正在生成**。

---

## 一·五、出问题先看自检

两处入口，同一套检查：

| 入口 | 适合 |
|---|---|
| 菜单栏 **◎ → 自检…** | 平时用，弹窗直接说哪儿不对、该点哪里 |
| 双击 **`体检.command`** | 想在窗口里看完整结果、方便复制给别人 |

它检查 7 项：辅助功能权限 / 桥接端口 / Photoshop 有没有开 / 插件目录 / 旧卫星插件有没有在抢连接 / 快捷键 / 配置文件。

**每一项都给出「怎么修」**，不是只报「有问题」。比如：

```
❌ 检测到 12 次状态解析失败
     几乎可以肯定是旧的「卫星插件」还挂在 UDT 里、连着同一个端口。
     处理：在 UDT 里 Unload 掉 com.huanmeng.ai.satellite / .probe，
           只保留 com.huanmeng.ai.retouch。
```

也用命令行体检（内容一样，权限那一项仅供参考）：

```bash
"/Users/zero/Documents/Dream AI/HuanmengRing/native/build/HuanmengRing.app/Contents/MacOS/HuanmengRing" --doctor
```

---

## 二、圆环怎么用

### 唤出的两种方式

| 方式 | 操作 | 需要权限 |
|---|---|---|
| 快捷键 | 按 **⌥⌘R** | **不需要** |
| 鼠标 | 按住 **⌥** 再点 **右键** | 辅助功能 |

**⌥右键在任何程序里都能用**，不限于 Photoshop —— 助手现在是个独立软件，
PS 没开的时候也得能唤出来。普通右键（不带 ⌥）任何情况下都不受影响。

> 嫌它在别的软件里碍事？偏好设置 → 交互 →「只在 Photoshop 里响应 ⌥右键」勾上。
>
> 想换快捷键（比如改成 ⌥R）：偏好设置 → 交互 → 热键，填 `alt+r`。
> 但提醒一句：**光秃秃的 ⌥R 是个正常的打字组合**（美式键盘上打出 ®），
> 全局接管之后所有软件里都按不出它了 —— 建议保留 ⌘ 或 ⌃ 这种修饰键。

**按一下 ⌥右键就松手**，圆环会留在原地，不用一直按着键。

### 键位

| 按键 | 作用 |
|---|---|
| 移动鼠标 | 高亮指针指向的扇区（只是高亮，不会自动进下一级） |
| **左键点击** | 确认。带子菜单的扇区会进入下一级 |
| **1 ~ 9** | 直接选中第 n 个扇区 |
| **←** / **Delete** | 返回上一级 |
| **Esc** | 整个圆环直接关掉 |
| **点圆心** | 返回上一级；已经在最外层时 = 关闭 |
| **点环外** | 关闭 |

> 悬停只负责高亮、点击才进下一级 —— 这是刻意的。
> 如果悬停就展开，鼠标路过带子菜单的扇区时会一路误触发。

### 六个扇区

| 扇区 | 点击后发生什么 |
|---|---|
| **生成** | 按当前面板参数跑一次生成，完成后右下角弹提示并把结果放回文档 |
| **参数** | 进下一级，可以改 **模型 / 分辨率 / 数量**，改完面板上的下拉同步跟着变 |
| **预设** | 进下一级 → 选分类 → 选预设，直接把提示词填进面板 |
| **对话** | 进下一级，圆环下方直接出结果（见下一节） |
| **读选区** | 把当前选区的像素读进插件 |
| **关闭** | 收起圆环（**不会退出助手**，退出助手在菜单栏里） |

带子菜单的扇区，**外缘会有一个朝外的小三角**，一眼能看出哪些能点进去。

### 在圆环里直接对话

点「对话」进下一级，里面有六项：

| 项 | 作用 |
|---|---|
| **模型** | 选对话模型。选项和当前值都跟着面板走 |
| **快捷提问** | 预设问题，一行一条（在插件设置 → 15. 圆环按钮 → 对话 · 快捷提问里改） |
| **打字提问** | 圆环下方弹出一个输入条，**可以直接打中文** |
| **看回复** | 把上一条回复重新弹出来（只在本次会话内有） |
| **新对话** | 清空聊天记录 |
| **打开面板** | 切到插件的对话页，接着聊 |

**打字提问支持中文输入法**（拼音、双拼、手写、emoji 都行）：

1. 点「打字提问」，圆环下方出现输入条，光标已经在里面
2. 正常打字 —— 输入法候选窗会出现在输入条那里，用它选词
3. **回车发送**；输入法正在组字时回车是「选词」，不会误发半截拼音
4. 问过一句之后，气泡会变成**对话窗**：上面是整段来回的聊天记录（可滚动，
   往上翻能看到之前的问答），下面还是输入框，接着问就行
5. **Esc 关掉气泡**（组字中的 Esc 先给输入法取消候选）

> 聊天记录是**插件推过来的**，和面板「对话」页里那份是同一份 ——
> 你在圆环里问的、在面板里问的混在一起，不会分成两笔账。
> 想看历史又不想问新问题，用圆环的「对话 → 看对话」。

**气泡可以拖**：按住气泡的空白边缘（不要按在文字上）左键拖动即可。
拖走之后它会**一直待在你放的地方** —— 接着问下一句、切到回复，都不会跳回圆环下面，
方便你把它挪开看画布。关掉气泡再打开，会重新回到圆环下方。

> 为什么打字时 Photoshop 会暂时失去焦点：输入法只能挂在**前台窗口**上，
> 这是系统的硬性要求，绕不过去。关掉气泡时助手会把焦点还给 Photoshop。

> 打字期间圆环自己不吃键盘了（数字键、Esc 都让给输入法），
> 也不响应「点环外关闭」—— 否则用鼠标点一下输入法候选词，
> 整个圆环就被当成「点了 Photoshop」收掉了。

**扇区名称哪来的？** 默认由插件下发（插件里写的是"生成/参数/预设/对话/读选区/关闭"），
所以圆环的用词永远和面板一致。想改成自己习惯的叫法，见下面「内容」标签页。

---

## 二·五、没有 Photoshop 的时候也能用

助手以前只是个「遥控器」：菜单显示在圆环上，活全在 Photoshop 插件里干。
PS 一关，它就成了什么都点不动的饼。现在补上了另一半：

| | 插件在 | 插件不在 |
|---|---|---|
| **对话** | 走插件（和面板共用同一份聊天记录） | **助手自己调接口**，记录存在本地 |
| **生成** | 走插件（结果能贴回文档） | **助手自己出图**，存到 `~/Pictures/幻梦AI` |
| 读选区 / 回写文档 | 有 | —（这两件事的本质就是操纵 PS 文档） |

**没插件时怎么出图**：圆环 →「生成」→ 弹出输入条 → 写提示词 → 回车。
出好的图会弹一个结果窗：能看、能拖、能「另存为」、能「打开文件夹」。

### 六个格子在独立模式下分别是什么

没插件时圆环不是残废的 —— 每一格都有自己的数据源：

| 格子 | 没插件时 |
|---|---|
| **生成** | 展开：纯文字 / 导入图片 / 截屏 / 用上次的图 |
| **参数** | 模型 / 尺寸 / 张数，**写进助手自己的配置**（`config.api`），立刻生效 |
| **预设** | 助手自己的预设库（`~/.huanmeng-presets.json`，首次自动装 8 条） |
| **对话** | 模型 / 快捷提问来自 `config.api`；打字提问、看对话、新对话都能用 |
| **读选区** | 没有 PS 选区可读，换成「导入图片 / 截屏」—— 同一件事，换个来源 |
| **关闭** | 不变 |

**预设从哪来**：插件的全部预设（68 条 / 23 个分类）**已经打包进 App**，
新机器装上就自带，不用先连插件导一次。存放在 `~/.huanmeng-presets.json`，
可以手改、也可以从插件导入覆盖。

> 打包的那份在 App 的 `Contents/Resources/presets.json`。
> 更新它：把新的 `yushe.json` 拷成 `native/HuanmengRing/Resources/presets.json`，
> 并把 `PresetStore.seedVersion` 加一（用户那边才会换新）。

**从插件导入**（菜单栏 ◎ →「从插件导入接口配置」）会把密钥、模型、
**对话模型清单、快捷提问、以及全部预设**一起拉过来 —— 插件里配过的东西，
助手这边不用再配一遍。

> 预设存在 `~/.huanmeng-presets.json`，格式 `[{category, name, prompt}]`，
> 可以手改、也可以直接从插件导入覆盖。

### 参考图和预设可以一起用

最常见的用法就是**截一张图 + 套一个预设** —— 比如截下人像，套「一键修脸」。
顺序随便，两个都会留着：

**先截图再套预设**
1. 圆环 → 生成 → 截屏 → ⇧⌘4 截一张
2. 气泡上会写「参考图：xxx · **Esc 回圆环可选预设**」
3. 按 Esc，再唤出圆环 → 预设 → 人像精修 → 一键修脸
4. 提示词被预设填好，**参考图还在**，回车生成

**先套预设再截图**
1. 圆环 → 预设 → 选一个（提示词填进气泡）
2. 按 Esc → 圆环 → 生成 → 截屏 → 截一张
3. **刚才的提示词不会被清掉**，参考图也挂上了，回车生成

气泡底部那行会同时写着「参考图：xxx · 预设：yyy」，一眼能看出挂了什么。

> 想回到纯文字：圆环 → 生成 → 纯文字。它只丢掉参考图，
> **不会清掉你已经打好的提示词**（那多半是刚套的预设，清了就白套了）。

### 独立出图：纯文字 / 导入图片 / 截屏

没有插件时点圆环的「生成」，会先展开一层问你**要不要参考图**：

| 选项 | 干什么 |
|---|---|
| **纯文字** | 只用提示词出图 |
| **导入图片** | 弹文件选择框，选一张图当参考（改风格、换背景、重画） |
| **截屏** | **等你自己截一张**，助手自动接住 |
| **用上次的图** | 接着上一张参考图再生成（改提示词重画时用） |

选完（或截屏收到后）会弹出输入条写提示词，**回车开始生成**。

**截屏是怎么工作的**：点「截屏」后助手进入等待状态，气泡上会告诉你截图存到哪个目录
（默认桌面）。然后你**用平时习惯的方式截图** —— ⇧⌘4、微信、Snipaste 都行 ——
只要它落盘，助手就自动接住并弹出输入条。

> 为什么不自己做截图：那样需要「屏幕录制」权限，又是一个系统授权框。
> 你有自己的截图习惯，助手只要接住就行，不用再学一套、也不用再授权一次。
>
> 注意：**只把图放剪贴板**的截图方式（比如某些工具默认 Ctrl+C）收不到，
> 要让截图**保存成文件**。

参考图会先缩到长边 2048 像素再发（截图动辄 3000+ 宽，原样上去请求大得离谱）。

---

**没插件时的参数**：圆环 →「参数」里改的**模型 / 尺寸 / 张数**直接写进助手自己的配置
（`~/.huanmeng-ring.json` 的 `config.api`），立刻生效。
张数是「挨个调 N 次」，会花 N 份额度 —— 菜单上标着「×N 张」，看清楚再选。

### 让助手自己会干活：导入接口配置

助手要独立工作，得有自己的密钥（插件那份存在 PS 的 localStorage 里，外面读不到）。

**插件开着的时候**：菜单栏 ◎ →「**从插件导入接口配置**」，一键把密钥、区域、
模型、系统提示词全拉过来，存进 `~/.huanmeng-ring.json`（权限 600，只有你能读）。

**插件不在的时候**：菜单栏 ◎ →「打开配置文件」，自己填 `config.api` 那一段：

```json
"api": {
  "grsApiKey": "sk-…",          // GRS 密钥（对话和生图共用同一把）
  "grsRegion": "domestic",       // domestic = grsai.dakka.com.cn / overseas = grsaiapi.com
  "grsBaseUrl": "",              // 自建网关时填，填了就不用 region
  "imgModel": "nano-banana-fast",
  "imageSize": "1K",             // 1K / 2K / 4K
  "aspectRatio": "auto",         // auto 或 1:1 / 4:3 / 16:9…
  "chatModel": "grs/gpt-5.5",
  "systemPrompt": "",            // 对话的系统提示词，可留空
  "outputFolder": ""             // 留空 = ~/Pictures/幻梦AI
}
```

> ⚠️ 这个文件里是**明文密钥**。助手已经把它设成「只有本人可读」，
> 但别把它拷进任何要分享的目录（比如这个 git 仓库）。

### 连不上时怎么自查

终端里跑这三条，出问题会直接告诉你是哪一段：

```bash
APP=~/Applications/HuanmengRing.app/Contents/MacOS/HuanmengRing

"$APP" --chat-test     # 密钥/地址/对话接口通不通
"$APP" --image-test    # 走一遍完整的生图链路（会消耗一点额度）
"$APP" --selftest      # 图像提取逻辑自检（不需要联网，不花额度）
```

---

## 三、偏好设置

菜单栏 → **偏好设置…**（或按 **⌘,**）

顶部是**实时预览**，任何一项改完立刻反映到预览和真圆环上，**关窗自动保存**。

### 外观

| 分组 | 项目 |
|---|---|
| 尺寸与形状 | 环大小 200–900 / 环带厚度比 / 扇区间隙比例 / 间隙上限 / 悬停弹出距离 / 整体不透明度 |
| 动画 | 出现起始缩放 / 出现速度 / 高亮渐变速度 |
| 投影 | 开关 / 模糊半径 / 垂直偏移 / 浓度 |
| 字号 | 扇区文字 / 扇区行高 / 圆心主标题 / 圆心副标题 |
| 禁用态 | 填充透明度 / 描边透明度 |
| 配色 | 11 个颜色，逐个可调 |

**配色有 5 套预设**（默认深灰 / 高对比 / 暖色 / 冷色 / 半透明），
顶部按钮一键套用，再逐项微调。

颜色写 `#RRGGBB` 或 `#RRGGBBAA`。**写错的那一项会退回它自己的默认色**，
不会整块变白 —— 这是刻意的，统一回退成白色反而更难排查。

### 交互

- **全局热键**：写法 `ctrl+alt+r`，修饰键可用 `ctrl` / `alt` / `shift` / `cmd`。
  留空则用默认的 **⌥⌘R**。
- **⌥右键唤出**：关掉后连辅助功能权限都不会去申请（没有这个功能就不该弹授权框）。
- **在鼠标位置弹出**：关掉则弹在主屏中央。
- **悬停即高亮**：关掉则指针经过只移圆心文字，不高亮扇区，只有点击才有效果。
- **数字键 1-9 直选 / Esc 关闭 / 悬停时向外弹出**
- **外缘三角标 / 方向指示线**

### 内容

- **采用插件下发的扇区名称**（默认开）：开着时用插件推过来的名字；
  插件没推的扇区用你自己设的名字。关掉则永远用自定义名。
- **六个扇区**：每个都能改 **显示 / 名称 / 顺序**。
  - 顺序数字越小越靠前，从**正上方**开始顺时针排。
  - 名称留空 = 用内置默认名。
  - **六个全关掉时会自动回退到内置六个** —— 空菜单会让圆环整个画不出来，
    看起来像程序挂了。

### 高级

- **桥接端口**（默认 8799）：**改完要重启助手才生效**，
  而且插件侧的 `src/features/ring-bridge/ring-bridge.js` 里的 `URL` 也要同步改。
- **配置文件路径** + 打开位置。
- 底部一排按钮：**恢复默认 / 导入配置… / 导出配置… / 打开配置文件 / 关闭**。

---

## 四、配置文件

路径：`~/.huanmeng-ring.json`

```bash
open -e ~/.huanmeng-ring.json     # 直接编辑
```

也可以在菜单栏菜单里点「打开配置文件」在访达里定位。

### 结构

```json
{
  "hotkey": "alt+cmd+r",
  "config": {
    "version": 1,
    "appearance": { "ringSize": 340, "colors": { "hoverFill": "#4278C2FF" } },
    "interaction": { "hotkey": "", "altRightClick": true },
    "content": { "usePluginLabels": true, "sectors": { "generate": { "label": "", "visible": true, "order": 0 } } },
    "bridge": { "port": 8799 }
  }
}
```

- **除 `config` 外的键由你自由支配** —— 助手写盘时会保留它不认识的键，不会整份覆盖。
- **缺任何一个字段都能正常启动**，按默认值补齐。
- **文件坏了也不会启动失败**：JSON 语法错、类型不对、只写一半，都会回退到默认值。

### 手改示例

```jsonc
{
  "config": {
    "appearance": { "ringSize": 480, "colors": { "hoverFill": "#E0614D" } },
    "content": { "sectors": { "chat": { "visible": false }, "generate": { "label": "出图" } } },
    "bridge": { "port": 8799 }
  }
}
```

改完**重启助手**生效（偏好设置窗口里的改动是即时生效的，手改文件不是）。

### 导入 / 导出

偏好设置 → 高级 → 「导出配置…」把文件存出来，「导入配置…」在另一台机器上导入。
导入前会先校验 JSON 合法性，坏文件不会把现有配置冲掉。

---

## 五、开机自启

```bash
cd "/Users/zero/Documents/Dream AI/HuanmengRing"

bash native/autostart.sh install     # 开机自动启动
bash native/autostart.sh status      # 查看状态
bash native/autostart.sh uninstall   # 取消自启动
```

原理是往 `~/Library/LaunchAgents/com.huanmeng.ring.plist` 放一个 LaunchAgent，
用户级配置、不需要管理员权限、随时可删。

> 重新 build 后路径不变，无需重装；**但如果移动了项目目录，要重新 install 一次**。

---

## 六、常见问题

**先点菜单栏 → ◎ →「自检…」。** 下面的表格是自检里各条对应的处理办法。

### 圆环弹出来了，但显示「未连接 / 插件」

点自检，看是下面哪一种：

| 自检说什么 | 真正的原因 | 怎么办 |
|---|---|---|
| 「插件未连接（端口空闲）」 | PS 里的插件没在跑 | UDT 里 Load `Dream-ps-ai`，再在 PS 里打开面板 |
| 「检测到 N 次状态解析失败」 | **旧卫星插件在抢端口**（最常见） | UDT 里 Unload `com.huanmeng.ai.satellite` / `.probe`，只留 `com.huanmeng.ai.retouch`，然后 Reload |
| 「端口被占用，但插件没连上」 | 有另一个助手实例在跑 | 菜单栏 → 退出幻梦圆环，然后重新双击 `安装.command` |

对应归档目录：`~/Documents/Dream AI/_archive/satellite-plugin/`（已废弃，别再 Load）。
手动查端口：`lsof -nP -iTCP:8799 -sTCP:LISTEN`

### ⌥右键没反应

- 点自检看权限那一行。没授权就点菜单栏那行「⚠️ 点这里授予…」，
  它会直接跳到系统设置页面，**勾完不用重启**。
- 重新 build 过之后要**重新授权**（ad-hoc 签名的固有行为）。
- 别的软件（某些截图/窗口管理工具）抢了全局右键也会冲突。
- 直接改用 **⌥⌘R**，它不需要任何权限。

### ⌥⌘R 没反应

- 被别的 App 占了这个组合。改成别的，例如 `ctrl+alt+r`：
  偏好设置 → 交互 → 全局热键。
- 或直接在配置文件里写 `"hotkey": "ctrl+alt+cmd+r"`。

### 圆环位置/大小不对

- 位置：默认在鼠标处弹出。想固定在屏幕中央，关掉偏好设置里的「在鼠标位置弹出」。
- 大小：外观 → 环大小。窗口会自动往回挪，不会跑出可见区域。

### 点了「生成」没反应

- 助手菜单栏图标应该变成 **◐**（运行中）。没变说明指令没送到 —— 看上面第 1 条。
- 面板必须已经初始化完（先开一次面板再唤圆环）。
- 生成有 5 分钟超时；超时会弹「等待生成结果超时」。

### 想看日志

日常排查用「自检…」就够了，**不需要看日志**。真要深挖才用终端启动：

```bash
"/Users/zero/Documents/Dream AI/HuanmengRing/native/build/HuanmengRing.app/Contents/MacOS/HuanmengRing"
```

重点看这几条：
- `[幻梦圆环] 插件已连接` —— 链路通了
- `[幻梦圆环] state 解析失败：…` —— 多半是旧卫星插件在捣乱
- `[幻梦圆环] 尚未获得辅助功能权限` —— 去授权

### 改完配置外观没变

- 偏好设置里的改动是**即时**的；手改 JSON 文件要**重启助手**。
- 如果改成 `ringSize` 后没变，检查有没有超出 200–900，超出会被钳制。

### 圆环上少了一项 / 想知道点下去发的是什么

圆环是**画出来**的，不是窗口控件 —— 「某个扇区为什么没出现」在界面上看不出来。
有一条命令能把当前配置下会画出来的菜单结构打印出来：

```bash
"/Users/zero/Documents/Dream AI/HuanmengRing/native/build/HuanmengRing.app/Contents/MacOS/HuanmengRing" --dump-menu
```

输出长这样（节选）：

```
4. 对话  — grs/gpt-5.4
    动作: （无 —— 点了不会有反应）
  (展开)  [5 项]
     1. 模型 grs/gpt-5.4  — 6 个可选
         动作: （无 —— 点了不会有反应）
        (展开)  [6 项]
           1. grs/gpt-5.4  — grs/gpt-5.4  ← ✓
               动作: 发指令 chatModel {value=grs/gpt-5.4}
     3. 打字提问  — 支持中文输入法
         动作: 圆环本地：打开输入条（支持中文输入法）
```

一行就是一个扇区：**名称 — 圆心提示**，下面一行是点下去会发生什么
（`发指令 xxx` = 发给插件；`圆环本地` = 助手自己处理）。带 `(展开)` 的还有下一级。

也可以用插件真实推过来的状态跑，排查「插件推的字段助手没认」这类问题：

```bash
# 把插件推的 state 报文存成文件（在插件里加一行 console.log，或用 UDT 的调试通道）
".../HuanmengRing" --dump-menu /tmp/state.json
```

> 状态报文里的字段名如果和助手期望的对不上，**不会报错**，只会安静地少一项。
> 所以「菜单里没有某项」时，先跑这个把结构打出来看。

---

## 七、重新构建

改了代码之后，**再双击一次 `安装.command` 就行**（它会关掉旧实例再开新的）。

想用命令行：

```bash
cd "/Users/zero/Documents/Dream AI/HuanmengRing"
bash native/build.sh          # 构建
bash native/build.sh run      # 构建 + 启动
bash native/package.sh        # 打包成 dist/幻梦圆环-<版本>.dmg
```

> **移动过项目目录的话，先删掉 `native/HuanmengRing/.build`**，
> 否则 Swift 的模块缓存里还是旧绝对路径，会报
> `PCH was compiled with module cache path ...`。

---

## 八、卸载

```bash
bash native/autostart.sh uninstall                       # 取消自启动
rm -rf "/Users/zero/Documents/Dream AI/HuanmengRing"     # 删助手
rm ~/.huanmeng-ring.json                                 # 删配置（可选）
```

再到 **系统设置 → 隐私与安全性 → 辅助功能** 里把 HuanmengRing 移除。
插件那边在 UDT 里 Remove Plugin 即可。
